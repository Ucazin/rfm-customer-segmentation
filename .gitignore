# Raw + processed data — regenerable from src/load_data.py
data/

# DuckDB warehouse (if you build one)
*.duckdb

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# NOTE: outputs/ ARE committed (4 PNGs + PLAYBOOK.md) so the deck is visible
# without running anything.
